# Movies-API
Movies API is a little Spring Boot backend project exploring role-based data delivery (USER vs ADMIN) and compares JPA and JDBC approaches for database access, highlighting different persistence strategies within a single application.
